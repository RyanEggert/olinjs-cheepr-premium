<div class="cheep" user="{{newcheep.username}}" id="{{newcheep._id}}">
  <b>{{newcheep.username}} said... </b> <button type="button" class="delete" id="{{newcheep._id}}"user="{{newcheep.username}}">Delete</button>
  <p>{{newcheep.words}}</p>
</div>
